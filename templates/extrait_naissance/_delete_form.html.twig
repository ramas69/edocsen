<form method="post" action="{{ path('extrait_naissance_delete', {'id': extrait_naissance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ extrait_naissance.id) }}">
    <button class="btn">Delete</button>
</form>
